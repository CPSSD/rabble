version: '2'

services:
  skinny:
    build:
      context: ./skinny
      dockerfile: Dockerfile
    volumes:
      - .:/repo
  example_microservice:
    build:
      context: ./services/example_microservice
      dockerfile: Dockerfile
    volumes:
      - .:/repo
